# Artifact manifest — dose + permuted-label files

Local dir: `experiments/training-run/data/full/` (**gitignored** — this
manifest is the tracked record; there is no separate `dose_perm/`
directory — these two files are physically inside `data/full/` alongside
the files in [`data_full.md`](data_full.md), and their entries live in the
same `data/full/report.json`, sha256
`98268c966740db3fea5091db7a40f5922494651140ffc6edcfb151e00fe228a0`). Split
into its own manifest because the plan's four-family split was
owner-approved; treat this as a subset view of `report.json`, not a
distinct source of truth.

| file | rows | order_hash (sha256 of ordered content tuples — NOT a file hash) | file sha256 (this local copy) | bytes | generated by (script @ commit) | relay |
|---|---|---|---|---|---|---|
| `D_inst_perm.parquet` | 200,000 | `5247139cf283574f181a59a0dbe98fa09be843204dd6a3a73f2bb53672219fce` | `55e842382e2ca9ec72a9fc1b8973d3bb1629fbbe5b1c0136ba8405349bb512ed` | 8,824,336 | `datagen/make_data.py` @ `00656db` (2026-07-26) | NOT on HF (owner-held as of 2026-07-26 per `docs/runbooks/phase2-runbook.md`) — confirmed absent from the 2026-07-29 relay listing |
| `D_dose_mult.parquet` | 16 | `8ddda6d64683deb6b32d8df20b42cb298e595b12a461c7a25c734c5007eed024` | `b0988e8e4185705257bb681eba619646f631e5b4e68c7b3a586594c5314af6ca` | 11,320 | `datagen/make_data.py` @ `00656db` (2026-07-26) | absent from the 2026-07-29 relay listing |

## Cross-run facts (from `report.json`)

- `D_inst_perm` `label_coincidence`: **0.000145** (0.0145%), pinned at
  seed 20260717's generation logic. Disjoint from `D_target`, `D_algo`,
  `D_target_eval`, and `probe` (verified by hash cross-check).
- `D_dose_mult` `disjoint_from D_inst`: **true** (`D_dose_mult ∩ D_inst =
  0`, per `notes/decisions.md`).

## Notes

- `order_hash` is defined in `geode/arith/validate.py` (`order_hash`) and is
  what `load_frozen_parquet` (V5.70) verifies at load time. Byte-identical
  parquet regeneration is NOT guaranteed across pyarrow versions;
  `order_hash` is the semantic identity, the file sha256 identifies this
  exact local copy.
- Recompute check: `python -c "import pandas as pd; from geode.arith.validate
  import order_hash; print(order_hash(pd.read_parquet('<path>')))"`.
