# Artifact manifest — data/full

Local dir: `experiments/training-run/data/full/` (**gitignored** — this
manifest is the tracked record). Seed `20260717`. Source of truth for
semantic hashes: `data/full/report.json` (sha256
`98268c966740db3fea5091db7a40f5922494651140ffc6edcfb151e00fe228a0`),
generated by `datagen/make_data.py`.

This manifest covers 5 of the 7 files in `data/full/`. The other 2
(`D_inst_perm.parquet`, `D_dose_mult.parquet`) are in
[`dose_perm.md`](dose_perm.md) — both are physically in this directory and
entries in this same `report.json`, split out because the plan's
four-family manifest split was owner-approved.

| file | rows | order_hash (sha256 of ordered content tuples — NOT a file hash) | file sha256 (this local copy) | bytes | generated by (script @ commit) | relay |
|---|---|---|---|---|---|---|
| `D_algo.parquet` | 1,000,000 | `48d4feffd1e50a457d4ce59624ba507dbd60e48b9c98e842c585715ad47769c9` | `e0360de5d01bd739992c5194c696528c45ab1c94ee48a677e255f82f4a8f5ed5` | 42,589,153 | `datagen/make_data.py` @ `bc8211a` (2026-07-18; script then lived at `scripts/make_data.py`) | `mhieuuu/elicit-vs-teach-arith` (public HF dataset) — present |
| `D_inst.parquet` | 1,000,000 | `d014388ab906c3db50fc2504fde2f38ff1d922e3826899713fbb161746caa048` | `d67b3e4a292519bcc35f28446f4a3f763ddb8462fb479b77696e64ae7cb350af` | 53,660,643 | `datagen/make_data.py` @ `bc8211a` (2026-07-18) | present |
| `D_target.parquet` | 1,000,000 | `69e3b09e2dd599e4ad8948fe2a5a19e67989be51bc006e8d4e220818ce16d0f7` | `a05ae85ede73235716980e8ee15709c25f42a75341af90e9968e4f1c8fdb10a1` | 40,951,228 | `datagen/make_data.py` @ `bc8211a` (2026-07-18) | present |
| `D_target_eval.parquet` | 100,000 | `588da81e0c016b8f0b3575a3aa7ad9b8e4c4421352a46fb4a30cd9ac713f6cb8` | `31ba4c74491d647357858aeb3c4bfba498e20924925a59db683914d7c78a3458` | 4,673,195 | `datagen/make_data.py` @ `ef0d46b` (2026-07-22) | present |
| `probe.parquet` | 1,024 | `2b6d51c27fce0e69b6b0b7d2f033fcc720e39ade287bb31350cb6b2f6fb562e2` (this is `probe_set_hash`, not `order_hash`) | `1c828ffae64df350e0b94cdcc3b60681d8f7d48b18add8583d919e45064ec9a9` | 66,672 | `datagen/make_data.py` @ `bc8211a` (2026-07-18) | present |

Relay status verified 2026-07-29 via
`huggingface_hub.list_repo_files("mhieuuu/elicit-vs-teach-arith", repo_type="dataset")`:
`D_algo`, `D_inst`, `D_target`, `D_target_eval`, `probe`, `report.json` all
present (`.gitattributes` also present, not a data file). `D_inst_perm` and
`D_dose_mult` are absent from the relay — see `dose_perm.md`.

## Cross-run facts (from `report.json`, not hand-computed)

- `D_target_eval` is disjoint from `D_target`, `D_algo`, and `probe`
  (verified by `order_hash`/`probe_set_hash` cross-check in `report.json`'s
  `disjoint_from` block).
- `D_inst`'s random labels coincide with the true answer in **~0.07%** of
  rows (7/10k at pilot scale, ≈700/1M at full scale) — recorded in
  `notes/decisions.md` as accepted **won't-fix**: it does not bias the
  A-vs-B comparison. This is a decisions.md-recorded measurement, not a
  `report.json` field.

## Notes

- `order_hash` is defined in `geode/arith/validate.py` (`order_hash`) and is
  what `load_frozen_parquet` (V5.70) verifies at load time. Byte-identical
  parquet regeneration is NOT guaranteed across pyarrow versions;
  `order_hash` is the semantic identity, the file sha256 identifies this
  exact local copy.
- Recompute check: `python -c "import pandas as pd; from geode.arith.validate
  import order_hash; print(order_hash(pd.read_parquet('<path>')))"`.
