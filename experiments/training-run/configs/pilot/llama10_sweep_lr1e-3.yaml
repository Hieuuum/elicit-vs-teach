# Llama target LR sweep @ 1e-3 — upper bracket point (the 38.7M-scale
# winner; plausibly too hot at 1B). See llama10_sweep_lr1e-4.yaml for the
# sweep design and launch command; this file differs only in run_id and
# train.lr.
run_id: evt-run10-sweep-lr1e-3
experiment:
  match_data_order_with: null
data:
  n_examples: 100000
train:
  lr: 1.0e-3
  snapshots:
    n: 4
