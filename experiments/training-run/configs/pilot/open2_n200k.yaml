# OPEN(2) pilot, Arm B @ n=200K — target-dataset-size sweep (spec 02 §11-§12).
# Overlay over configs/run6_target.yaml; see open2_n10k.yaml for the full
# header (usage, prefix rationale, lr-pin prerequisite).
run_id: evt-run6-pilot-n200k
experiment:
  match_data_order_with: null  # G7 binds the production pair, not the pilot grid
data:
  n_examples: 200000           # prefix of the frozen order
train:
  snapshots:
    n: 20                      # pilot-sized schedule (spec 02 §11)
