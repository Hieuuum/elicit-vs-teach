# Run-2 smoke (de-risk only, ~$0): tiny slice + a few steps to exercise the
# whole train_sft.py path — parquet download + hash check, span conversion,
# parent-gate refusal, registration, masked SFT steps, manifest finalize —
# before any real launch. NOT a hyperparameter pilot.
run_id: evt-run2-smoke
experiment:
  parent_run_id: evt-run1-smoke-parent   # fixture parent registered by the smoke driver
data:
  max_rows: 2048
train:
  max_steps: 10
  eval_every: 5
