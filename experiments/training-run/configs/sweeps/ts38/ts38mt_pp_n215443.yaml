# ts38mt target-stage sweep point: n=215443, paper-protocol pretaught arm.
# Overlay over ../../ts38mt_pp.yaml. G7-matched against the shipped
# evt-ts38-base-n215443 run (train_target.py:411). eval_every/max_steps/
# min_steps identical to the shipped ts38pp_pretaught_n215443.yaml overlay
# (same recipe, only theta0/snapshots differ).
run_id: evt-ts38mt-pp-n215443
experiment:
  match_data_order_with: evt-ts38-base-n215443
data:
  n_examples: 215443
train:
  eval_every: 250
  max_steps: 34000
  stopping:
    min_steps: 1684
