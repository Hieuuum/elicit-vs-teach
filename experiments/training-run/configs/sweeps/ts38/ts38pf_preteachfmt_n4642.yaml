# ts38pf target-stage sweep point: n=4642. Overlay over
# ../../ts38pf_preteachfmt.yaml. Paired with ts38_base_n4642.yaml (G7:
# identical D_algo_bare stream, matched via match_data_order_with below —
# the base arm is reused, not retrained). eval_every/max_steps/min_steps
# identical to ts38mw_pretaught_n4642.yaml (same recipe, different theta0).
run_id: evt-ts38pf-preteachfmt-n4642
experiment:
  match_data_order_with: evt-ts38-base-n4642
data:
  n_examples: 4642
train:
  eval_every: 5
  max_steps: 1000
  stopping:
    min_steps: 37
