# ts38fs-tiny target-stage sweep point: install i=10, target n=21544, seed
# s=316 (the family's single seed -- decisions.md 2026-08-20 "ts38fs sweep
# killed mid-run, ts38fs-tiny extends the dose grid down to i=2/10").
# Overlay over ../../ts38fs_target.yaml. eval_every/max_steps/min_steps
# identical to ts38fs's own n=21544 schedule (same target-stage recipe, only
# theta0/install size differ) -- STEP_SCHEDULE in generate_ts38fs_overlays.py.
run_id: evt-ts38fs-tiny-i10-n21544-s316
experiment:
  parent_run_id: evt-ts38fs-tiny-parent-n10
data:
  n_examples: 21544
train:
  eval_every: 25
  max_steps: 5000
  stopping:
    min_steps: 169
