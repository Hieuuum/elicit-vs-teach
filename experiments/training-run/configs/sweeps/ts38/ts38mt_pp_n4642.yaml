# ts38mt target-stage sweep point: n=4642, paper-protocol pretaught arm.
# Overlay over ../../ts38mt_pp.yaml. G7-matched against the shipped
# evt-ts38-base-n4642 run (train_target.py:411). eval_every/max_steps/
# min_steps identical to the shipped ts38pp_pretaught_n4642.yaml overlay
# (same recipe, only theta0/snapshots differ).
run_id: evt-ts38mt-pp-n4642
experiment:
  match_data_order_with: evt-ts38-base-n4642
data:
  n_examples: 4642
train:
  eval_every: 5
  max_steps: 1000
  stopping:
    min_steps: 37
