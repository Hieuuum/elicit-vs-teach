# ts38 mini sweep point: n=316228. This run IS the G7 anchor — the same-size
# ts38_pretaught_n316228.yaml overlay points match_data_order_with at
# evt-ts38-base-n316228. Overlay over ../../ts38_base.yaml. eval_every/
# max_steps copied verbatim from the matching-size llama_fig2nl3 overlay
# (llama_fig2nl3_{inst,noinst}_n316228.yaml). min_steps = ceil(316228/128) =
# 2471 (require_full_epoch1 guard, ts38-mini guard 1). Edge rule (plan): a
# peak at this size reads "unresolved", not "no teaching".
run_id: evt-ts38-base-n316228
data:
  n_examples: 316228
train:
  eval_every: 375
  max_steps: 30000
  stopping:
    min_steps: 2471
