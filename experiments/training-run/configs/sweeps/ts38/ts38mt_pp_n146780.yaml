# ts38mt target-stage sweep point: n=146780, paper-protocol pretaught arm.
# Overlay over ../../ts38mt_pp.yaml. G7-matched against the shipped
# evt-ts38-base-n146780 run (train_target.py:411). eval_every/max_steps/
# min_steps identical to the shipped ts38pp_pretaught_n146780.yaml overlay
# (same recipe, only theta0/snapshots differ).
run_id: evt-ts38mt-pp-n146780
experiment:
  match_data_order_with: evt-ts38-base-n146780
data:
  n_examples: 146780
train:
  eval_every: 175
  max_steps: 23000
  stopping:
    min_steps: 1147
