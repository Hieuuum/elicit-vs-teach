# ts38mt target-stage sweep point: n=146780, base arm. Overlay over
# ../../ts38mt_base.yaml. G7-matched against the shipped
# evt-ts38-base-n146780 run (arm-agnostic order_hash/n_examples check,
# train_target.py:411) even though this run is itself a base arm — every
# ts38mt arm anchors to the same shipped run. eval_every/max_steps/
# min_steps copied verbatim from that run's own ts38_base_n146780.yaml
# overlay.
run_id: evt-ts38mt-base-n146780
experiment:
  match_data_order_with: evt-ts38-base-n146780
data:
  n_examples: 146780
train:
  eval_every: 175
  max_steps: 23000
  stopping:
    min_steps: 1147
