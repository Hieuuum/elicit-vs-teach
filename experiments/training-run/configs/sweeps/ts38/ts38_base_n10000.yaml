# ts38grid unified 3-arm grid extension (EXPERIMENTS.md §6.17), 2026-08-16 —
# densification point: n=10000. This run IS the G7 anchor — same-size
# ts38mw_pretaught_n10000.yaml/ts38pf_preteachfmt_n10000.yaml point
# match_data_order_with here. Overlay over ../../ts38_base.yaml.
# eval_every/max_steps copied verbatim from llama_fig2nl3_noinst_n10000.yaml; 20
# epochs = 1580 <= 2000, ceiling never binds. min_steps = ceil(10000/128) = 79
# (require_full_epoch1 guard, ts38-mini guard 1).
run_id: evt-ts38-base-n10000
data:
  n_examples: 10000
train:
  eval_every: 10
  max_steps: 2000
  stopping:
    min_steps: 79
