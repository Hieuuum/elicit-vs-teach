# ts38grid unified 3-arm grid extension (EXPERIMENTS.md §6.17), 2026-08-16 —
# densification point: n=46416. Overlay over ../../ts38mw_pretaught.yaml. Paired
# with ts38_base_n46416.yaml (G7: identical D_algo_bare stream, matched via
# match_data_order_with below — the base arm is reused, not retrained).
# eval_every/max_steps/min_steps identical to ts38_base_n46416.yaml (same
# recipe, different theta0).
run_id: evt-ts38mw-pretaught-n46416
experiment:
  match_data_order_with: evt-ts38-base-n46416
data:
  n_examples: 46416
train:
  eval_every: 55
  max_steps: 11000
  stopping:
    min_steps: 363
