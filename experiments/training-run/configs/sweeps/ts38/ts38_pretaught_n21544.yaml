# ts38 mini sweep point: n=21544. Paired with ts38_base_n21544.yaml (G7:
# identical D_algo_bare stream, matched via match_data_order_with below).
# Overlay over ../../ts38_pretaught.yaml. eval_every/max_steps copied
# verbatim from the matching-size llama_fig2nl3 overlay. min_steps =
# ceil(21544/128) = 169 (require_full_epoch1 guard, ts38-mini guard 1).
run_id: evt-ts38-pretaught-n21544
experiment:
  match_data_order_with: evt-ts38-base-n21544
data:
  n_examples: 21544
train:
  eval_every: 25
  max_steps: 5000
  stopping:
    min_steps: 169
