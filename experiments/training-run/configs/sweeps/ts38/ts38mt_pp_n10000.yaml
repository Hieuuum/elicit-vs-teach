# ts38mt target-stage sweep point: n=10000, paper-protocol pretaught arm.
# Overlay over ../../ts38mt_pp.yaml. G7-matched against the shipped
# evt-ts38-base-n10000 run (train_target.py:411). eval_every/max_steps/
# min_steps identical to the shipped ts38pp_pretaught_n10000.yaml overlay
# (same recipe, only theta0/snapshots differ).
run_id: evt-ts38mt-pp-n10000
experiment:
  match_data_order_with: evt-ts38-base-n10000
data:
  n_examples: 10000
train:
  eval_every: 10
  max_steps: 2000
  stopping:
    min_steps: 79
