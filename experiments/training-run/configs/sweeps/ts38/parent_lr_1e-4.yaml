# ts38-pretaught-parent fallback rung — use only if the primary 3e-4 pin
# (run-2 winner) misses G1 (capability on D_target_eval) or G8 (TinyStories
# retention) at launch. See ts38_pretaught_parent.yaml header note 2/3.
train:
  lr: 1.0e-4      # dot-mantissa: a bare exponent parses as a STRING (YAML 1.1)
