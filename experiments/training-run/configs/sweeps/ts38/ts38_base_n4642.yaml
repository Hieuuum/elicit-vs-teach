# ts38 mini sweep point: n=4642. This run IS the G7 anchor — the same-size
# ts38_pretaught_n4642.yaml overlay points match_data_order_with at
# evt-ts38-base-n4642. Overlay over ../../ts38_base.yaml. eval_every/
# max_steps copied verbatim from the matching-size llama_fig2nl3 overlay
# (llama_fig2nl3_{inst,noinst}_n4642.yaml). min_steps = ceil(4642/128) = 37
# (require_full_epoch1 guard, ts38-mini guard 1).
run_id: evt-ts38-base-n4642
data:
  n_examples: 4642
train:
  eval_every: 5
  max_steps: 1000
  stopping:
    min_steps: 37
