# ts38grid unified 3-arm grid extension (EXPERIMENTS.md §6.17), 2026-08-16 —
# densification point: n=2154. This run IS the G7 anchor — same-size
# ts38mw_pretaught_n2154.yaml/ts38pf_preteachfmt_n2154.yaml point
# match_data_order_with here. Overlay over ../../ts38_base.yaml.
# eval_every/max_steps copied verbatim from llama_fig2nl3_noinst_n2154.yaml; 20
# epochs = 340 <= 1000, ceiling never binds. min_steps = ceil(2154/128) = 17
# (require_full_epoch1 guard, ts38-mini guard 1).
run_id: evt-ts38-base-n2154
data:
  n_examples: 2154
train:
  eval_every: 5
  max_steps: 1000
  stopping:
    min_steps: 17
