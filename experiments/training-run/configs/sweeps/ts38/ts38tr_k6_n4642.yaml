# ts38tr target-stage sweep point: n=4642, k6 (6-of-8 blocks kept) arm.
# Overlay over ../../ts38tr_k6.yaml. G7-matched against the shipped
# evt-ts38-base-n4642 run (train_target.py:411). eval_every/max_steps/
# min_steps identical to the shipped ts38mt_pp_n4642.yaml overlay (same
# recipe, only theta0/snapshots differ).
run_id: evt-ts38tr-k6-n4642
experiment:
  match_data_order_with: evt-ts38-base-n4642
data:
  n_examples: 4642
train:
  eval_every: 5
  max_steps: 1000
  stopping:
    min_steps: 37
