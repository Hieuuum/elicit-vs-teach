# ts38grid unified 3-arm grid extension (EXPERIMENTS.md §6.17), 2026-08-16 —
# densification point: n=215443. This run IS the G7 anchor — same-size
# ts38mw_pretaught_n215443.yaml/ts38pf_preteachfmt_n215443.yaml point
# match_data_order_with here. Overlay over ../../ts38_base.yaml. eval_every
# copied verbatim from llama_fig2nl3_noinst_n215443.yaml; its max_steps 20208 is
# below 20 epochs, so max_steps is raised to 34000 — same >=20-epoch ceiling
# rule as commit 6b735f1 (n100000/n316228). min_steps = ceil(215443/128) = 1684
# (require_full_epoch1 guard, ts38-mini guard 1).
run_id: evt-ts38-base-n215443
data:
  n_examples: 215443
train:
  eval_every: 250
  max_steps: 34000
  stopping:
    min_steps: 1684
