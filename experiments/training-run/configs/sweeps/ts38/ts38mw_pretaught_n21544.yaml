# ts38mw target-stage sweep point: n=21544. Overlay over
# ../../ts38mw_pretaught.yaml. Paired with ts38_base_n21544.yaml (G7:
# identical D_algo_bare stream, matched via match_data_order_with below —
# the base arm is reused, not retrained). eval_every/max_steps/min_steps
# identical to ts38_pretaught_n21544.yaml (same recipe, different theta0).
run_id: evt-ts38mw-pretaught-n21544
experiment:
  match_data_order_with: evt-ts38-base-n21544
data:
  n_examples: 21544
train:
  eval_every: 25
  max_steps: 5000
  stopping:
    min_steps: 169
