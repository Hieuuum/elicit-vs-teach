# ts38mt target-stage sweep point: n=316228, format-pretaught arm. Overlay
# over ../../ts38mt_fmt.yaml. G7-matched against the shipped
# evt-ts38-base-n316228 run (train_target.py:411). eval_every/max_steps/
# min_steps copied verbatim from ts38_base_n316228.yaml (same recipe, only
# theta0/snapshots differ).
run_id: evt-ts38mt-fmt-n316228
experiment:
  match_data_order_with: evt-ts38-base-n316228
data:
  n_examples: 316228
train:
  eval_every: 375
  max_steps: 50000
  stopping:
    min_steps: 2471
