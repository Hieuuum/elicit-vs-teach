# 1M target-LR sweep @ lr 3e-3 — upper bracket point. See
# target_sweep_1m_lr3e-4.yaml for the sweep design (3-point bracket,
# winner rule, edge-extension rule) and launch command; this file differs
# only in run_id and train.lr.
run_id: evt-run8-sweep-lr3e-3
experiment:
  match_data_order_with: null  # G7 binds the production pair, not sweeps
train:
  lr: 3.0e-3
  max_steps: 7813              # fixed 1-epoch budget (owner 2026-07-24) — see lr3e-4
  snapshots:
    n: 0                       # sweeps save nothing (owner 2026-07-24)
