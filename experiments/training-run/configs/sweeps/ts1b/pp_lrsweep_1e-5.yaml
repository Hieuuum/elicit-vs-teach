# ts1b pp-parent LR mini-sweep rung (1e-5) — see pp_lrsweep_1e-4.yaml for
# the full rationale/mechanics (2000-step probe, plateau rule inert,
# min_steps 31093 inherited from ts1b_pp_parent.yaml, name kept distinct
# from the pretrain-stage lrsweep_*.yaml files). Bottom rung of the
# bracket; if this wins, extend one rung further down before pinning
# (nulls-need-bracketing rule — an endpoint winner is not yet a swept
# optimum).
run_id: evt-ts1b-pp-lrsweep-1e-5
train:
  lr: 1.0e-5
  max_steps: 2000
  eval_every: 200
