# fig2nl3 installer retry ladder, rung 3 (last) — the paper's target-stage
# pin. At n_dose=1 this LR halved retention (G2 0.1719); at dose16 the damage
# profile is unmeasured but the direction is known. A rung wins only by
# clearing BOTH bars; all rungs failing halts the family for owner triage —
# bars do not move (the fig2nl lesson).
train:
  lr: 3.53e-4     # dot-mantissa: a bare exponent parses as a STRING (YAML 1.1)
